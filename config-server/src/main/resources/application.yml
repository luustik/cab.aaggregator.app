spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/luustik/config-repo.git
          default-label: main
          search-paths: "{application}"
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8000
