spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/luustik/config-repo.git
          default-label: main
          search-paths: "{application}"

server:
  port: 8000
