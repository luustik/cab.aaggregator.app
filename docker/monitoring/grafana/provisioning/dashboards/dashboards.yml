apiVersion: 1

providers:
  - name: dashboards
    folder: dashboards
    type: file
    allowUiUpdates: true
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: true